name: physiocare_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.12.0

  camera: ^0.11.0+2
  google_mlkit_pose_detection: ^0.12.1
  google_mlkit_commons: ^0.8.1

  js: ^0.6.7

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true