/// Shared exercise type enum used by both mobile (ML Kit) and web (MediaPipe)
/// pose engines. Import this instead of defining ExerciseType locally.
enum ExerciseType {
  bicepCurl,
  sideRaise,
  squats,
}